<?xml version="1.0" encoding="UTF-8"?>
<svg width="120" height="40" viewBox="0 0 120 40" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Gradient for the circle -->
    <linearGradient id="circleGradientWhite" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:rgba(255,255,255,0.9);stop-opacity:1" />
      <stop offset="100%" style="stop-color:rgba(255,255,255,0.7);stop-opacity:1" />
    </linearGradient>

    <!-- Gradient for the lightning bolt -->
    <linearGradient id="boltGradientWhite" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#0d47a1;stop-opacity:1" />
      <stop offset="50%" style="stop-color:#388eff;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#0d47a1;stop-opacity:0.8" />
    </linearGradient>

    <!-- Glow effect -->
    <filter id="glowWhite">
      <feMorphology operator="dilate" radius="1"/>
      <feGaussianBlur stdDeviation="2" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Main circle -->
  <circle cx="20" cy="20" r="18" fill="url(#circleGradientWhite)" stroke="rgba(255,255,255,0.4)" stroke-width="0.5"/>

  <!-- Inner circle highlight -->
  <circle cx="20" cy="20" r="15" fill="none" stroke="rgba(255,255,255,0.2)" stroke-width="0.5"/>

  <!-- Lightning bolt -->
  <path d="M24 8 L16 20 L20 20 L16 32 L24 20 L20 20 Z"
        fill="url(#boltGradientWhite)"
        filter="url(#glowWhite)"
        stroke="rgba(13,71,161,0.3)"
        stroke-width="0.3"/>

  <!-- TDS Text -->
  <text x="45" y="17" font-family="SF Pro Display, -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, sans-serif"
        font-size="14" font-weight="700" fill="rgba(255,255,255,0.95)">TDS</text>

  <!-- RapidInt Text -->
  <text x="45" y="31" font-family="SF Pro Display, -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, sans-serif"
        font-size="11" font-weight="500" fill="rgba(255,255,255,0.8)" letter-spacing="0.5">RapidInt</text>

  <!-- Subtle background circle glow -->
  <circle cx="20" cy="20" r="19" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="2" opacity="0.5"/>
</svg>